import string
from random import
letters = string.ascii_letters
digits = sting.digits
symbols = String.punctuation
chars = leters + digits + symbols
min length = 8
max length = 16
 password = ".join(choice(chars) for x in range(randiet(min_length,max_length)))
 print (password)
